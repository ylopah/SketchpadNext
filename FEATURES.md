# SketchpadNext 功能清单

本清单记录已经可以在当前版本中使用的功能。产品范围明确不包含独立多边形工具、多边形内部、线标签和圆标签；点标签与独立文本保留。

## 动态几何

- 自由点，以及线段、射线、直线、圆和圆弧上的约束点
- 选择工具在近似交叉处直接创建精确、动态的交点；两个交点非常接近时也能分别点出
- 线段、射线、直线、圆、以线段为动态半径的圆、过三点圆；过三点圆保留动态圆心点，但默认不显示字母且不占用点名字母顺序
- 中点、中垂线、平行线、垂线、角平分线；角平分线支持两条公共端点边，中垂线和角平分线都可直接点出新点
- 三角形重心、内心、垂心和内切圆，均随三个顶点动态更新
- 圆上的弧、过三点的弧、圆内部、扇形内部、弓形内部
- 轨迹：对路径点驱动的依赖点进行动态采样
- 级联删除、隐藏、锁定，以及图片、图形、点和文字之间真正统一的前后层级顺序

## 选择、编辑和外观

- 几何画板式累加选择、再次点击取消、Shift 增减、空白框选；拖动尚未选中的对象时自动切换为仅选中该对象
- 自由点和路径点直接拖动；中点、交点、外心、变换点等派生点通过自由父对象整体平移；线、依赖图形和混合多选对象可拖动
- 独立文本、图片、数据对象、手绘标记和自由原点坐标系支持单选或多选拖动；点标签（含下标）可绕点小范围拖动
- 点名称、`A_1` / `A[1]` 下标、`x^2` 上标、Unicode 名称、常用几何与数学符号、批量点命名；名称输入在回车或失焦时提交，清空不会删除点
- 点标签拖动限制在点附近；默认点颜色为黑色
- 可持久化的点大小、颜色、标签默认值、线宽、颜色、虚实线、文本字号、点名字号、度量小数位和全局交互设置；编辑已选对象的单项样式不会污染新对象默认值
- `Ctrl+C` / `Ctrl+V` / `Ctrl+D` 复制、粘贴和原位复制；自动复制所需父对象并重建依赖
- 一次操作只生成一个撤销记录，支持重做
- 独立文本、图片插入与拖动；选择状态下双击文本可编辑，清空已有文本会删除对象并可撤销
- 鼠标滚轮连续缩放，空格/中键/右键平移；触屏首指安全仲裁、双指平移与捏合缩放
- 工具快捷键统一为 `Alt+字母`，并提供持久化总开关，避免文本输入时误触
- 开启吸附时同步显示吸附网格，关闭时同步隐藏

## 标识与信息

- 从顶点向夹角内拖动创建角标识；90° 自动显示直角方框
- 1～4 道角弧、角标识大小、透明度、方向箭头和反向角
- 线段等长刻痕、平行箭头、位置拖动、1～4 道循环
- 带实时笔迹预览的手绘标记与 Shift 擦除
- 信息工具显示对象类型、父对象、子对象及关键几何数值

## 构造、度量和变换

- 按当前选择批量构造线段、直线、射线、中点、全部交点、平行线、垂线、角平分线和三角形中心等；平行线和垂线按“所有已选点 × 所有已选线形对象”完整配对
- 距离、长度、弧长、线段比、角度、半径、圆周长、圆面积、坐标、斜率、路径点值和三点共线误差；结果注明具体被测对象
- 角标记、圆弧和任意两条线形对象均可直接测角
- 标记中心和镜面；平移、旋转、缩放和反射
- 任意派生线、圆弧、内部、轨迹和标记均可用统一动态变换像处理
- 重复最近变换 1～50 代，并作为一次操作撤销

## 数据与绘图

- 无单位、距离和角度参数；双击修改与连续动画
- 不使用 `eval` 的安全表达式计算器
- `sin`、`cos`、`tan`、反三角、`sqrt`、`abs`、`ln`、`log`、取整和符号函数
- 直角、矩形和极坐标网格；网格只在显式创建坐标系后出现并铺满画布
- 按表达式绘制动态坐标点
- `y=f(x)`、`x=f(y)`、`r=f(theta)`、导函数和参数曲线
- 计算、动态坐标点和各类函数图像按对象 ID 保存显式参数依赖；同名参数、复制和旧工程迁移不会悄悄改绑
- 动态数据表格与当前数值采集
- 点追踪和痕迹清除

## 演示与工作流

- 隐藏、显示、动画、移动、链接和声音动作按钮
- 自定义工具：保存完整选中构造并在其他画板中重新实例化
- 多页面工程，新建、切换、改名和删除页面
- 新建工程会检查全部页面是否含内容，避免从空白当前页误清空其他页面
- `.spn` 可编辑工程的保存、另存为、打开、刷新后文件关联恢复和 `Ctrl+S` 原位写回（内部为开放 JSON），与旧单页面 JSON 向后兼容
- 本地自动保存；图片输出按产品决策交由系统截图工具处理
- 一键复制当前页面、当前视图的完整 TikZ/LaTeX 静态作图代码；保留几何、标签、颜色、虚线、填充、坐标系、函数图、轨迹和标记
- 可直接双击、无需 CMD/Node.js 的 `SketchpadNext.html` 单文件版；开发时仍保留无第三方依赖的本地服务器
- 网页内置分类工具说明和设置面板；设置保存在当前浏览器，下次打开继续使用

## 验收

- 当前自动测试：89 项
- 测试覆盖几何算法、动态依赖、近距离双交点、三角形中心与内切圆、交点与圆弧定义域、轨迹、变换、表达式显式绑定、绘图、全局图层、触控视图变换、表格、动作、复制、图片、文本数学格式、设置持久化、TikZ 导出、派生点整体拖动和命中区域
- 已在内置浏览器实测说明/设置弹窗、吸附网格同步、Alt 快捷键及总开关、设置重载、默认黑点和控制台无报错

## 兼容边界

- 不读取专有 `.gsp` 二进制文件；工程格式为开放的 `.spn`（JSON）。
- 不复制旧软件代码和资源，只做功能层面的净室实现。
- 独立多边形工具、多边形内部、线标签和圆标签按用户的产品决策不实现。
