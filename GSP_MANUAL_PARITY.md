# 几何画板 5.06 手册对照结果

基准：`Sketchpad Help/几何画板使用手册.pdf`。对照目标是常用几何创作与演示能力，而不是专有 `.gsp` 二进制格式兼容。

| 手册功能组 | 对照结果 | SketchpadNext 实现 |
| --- | --- | --- |
| 选择与移动 | 完成 | 累加选择、取消、Shift 增减、框选、自由点/路径点直接拖动、派生构造整体拖动、双指平移/缩放、锁定 |
| 点、直尺、圆工具 | 完成 | 点击与按下拖出、15° 约束、路径点、动态半径圆 |
| 多边形工具与内部 | 不实现（产品决策） | 用户明确表示不需要 |
| 文本和标签 | 完成（约定范围） | 点标签、下标、Unicode、批量命名；清空独立文本即删除且可撤销；线/圆标签不实现 |
| 标识工具 | 完成 | 角/直角、方向、透明度、多弧、等长刻痕、平行箭头、涂鸦 |
| 信息工具 | 完成 | 类型、父子关系、位置、半径和角度 |
| 构造菜单 | 完成 | 点、中点、交点、线、平行/垂直/中垂/角平分、圆、弧和内部 |
| 轨迹与追踪 | 完成 | 动态轨迹采样、点追踪和痕迹清除 |
| 变换 | 完成 | 平移、旋转、缩放、反射、标记中心/镜面、多代重复变换 |
| 隐藏、显示与对象层级 | 完成 | 隐藏不破坏依赖、显示全部、锁定、图片/图形/点/文字跨类型前后顺序 |
| 动作按钮 | 完成 | 隐藏、显示、动画、移动、链接和声音 |
| 编辑 | 完成 | 复制、粘贴、原位复制、级联删除、撤销/重做 |
| 多页面 | 完成 | 页面新建、切换、改名、删除及工程保存 |
| 度量 | 完成 | 距离、长度、弧长、比值、角标记/圆弧/线形夹角、半径、周长、面积、坐标、斜率、点值；结果注明被测对象 |
| 参数与计算 | 完成 | 参数、单位、安全表达式、常用数学函数、按对象 ID 保存的动态依赖 |
| 表格 | 完成 | 多值表格与手动采集当前数值 |
| 坐标系与绘图 | 完成 | 直角/矩形/极坐标网格、数值点、三种函数模式、导函数、参数曲线 |
| 图片 | 完成 | 本地图片插入、选择、单选/多选拖动、复制并随 `.spn` 工程保存；不提供图片导出 |
| 自定义工具 | 完成（现代替代） | 将选中构造保存为可复用模板并重建依赖 |
| 文件与导出 | 完成（开放格式） | 多页面 `.spn` 可编辑工程、旧 JSON 兼容、自动保存；图片输出使用系统截图工具，不读取专有 `.gsp` |

## 交互约定

1. 构造遵循“先选择对象，再执行命令”，直接工具同时支持点击和拖出。
2. 三点角相关功能按 `边上点—顶点—边上点`，顶点位于中间。
3. 交点属于选择工具，不作为独立左侧工具。
4. 所有构造保存父子依赖，父对象移动后动态重算。
5. 坐标网格默认不显示；只有显式创建坐标系后才出现，并覆盖整个可视画布。
6. 所有需要输入的操作使用应用内对话框，不依赖浏览器不支持的原生 `prompt()`。
