# SketchpadNext 后续路线图构想

这份路线图面向动态几何、教学、证明、协作、AI、可视化、性能和移动端。优先级以“先夯实几何与操作模型，再扩展教学和智能能力”为原则。

## 三项底层架构原则

1. **稳定的几何内核**：对象使用稳定身份和显式依赖图；交点分支、退化情况、数值容差和增量重算必须可预测。
2. **结构化命令日志**：构造、拖动、样式与删除统一表示为可验证、可撤销、可回放的命令，而不只保存整份文档快照。
3. **能力与界面解耦**：教学检查、协作和 AI 只调用类型化几何 API，不直接操作 DOM、按钮或屏幕坐标，以便复用于单文件版、移动端和未来 3D 客户端。

## 近期：1～3 个迭代

### P0：可靠性与教学基础

- **稳定交点身份与候选切换**：交点接近、相切或分裂时保持分支连续；提供局部放大镜和候选轮换。
- **构造步骤时间线**：逐步播放、回退、隐藏后续步骤，并让一次复合构造只占一个步骤。
- **几何谓词检查器**：先覆盖重合、共线、平行、垂直、等长、等角和共圆，为练习自动判定奠基。
- **性能基线**：建立 1k、5k、10k 对象场景，记录帧耗时、命中测试和几何重算次数。

### P1：直接提升使用价值

- **练习模式**：锁定已知条件、限制可用工具、设置目标条件、提供分级提示并自动检查结果。
- **对象与依赖树**：查看父子关系、搜索对象、管理隐藏构造、批量重命名和删除。
- **参数滑块与动画时间轴**：统一参数动画、轨迹播放、暂停和演示录制。
- **PWA 与只读分享链接**：离线使用、版本化缓存、无需下载即可打开教师发出的题目。

### P2：移动端体验

- 长按候选菜单、触控放大镜、径向工具栏、手写笔支持、防掌触和轻量触觉反馈。
- 完整键盘导航、高对比度主题及可朗读的对象关系说明。

## 中期：3～9 个月

### P0：规模化能力

- **增量几何求值引擎**：依赖 DAG、几何缓存和脏标记，只重算受影响的后代对象。
- **强化自定义工具/宏**：把构造子图封装为“类型化输入 → 动态输出”，支持分享、版本和退化条件检查。
- **Web Worker 计算层**：把轨迹采样、复杂表达式、导出和证明搜索移出主线程，并支持任务取消。

### P1：教学与证明

- **教学题库与自动变式**：从约束生成非退化题目，固定知识点同时变化图形参数。
- **半自动证明助手**：依据依赖图和定理规则生成可核验的证明步骤，先覆盖三角形常用定理。
- **反例搜索**：拖动或参数扫描寻找不满足猜想的构型，帮助区分“看起来成立”和“必然成立”。

### P1：协作

- **对象锚定批注**：评论附着到对象或构造步骤，删除对象后保留可追溯状态。
- **课堂会话**：教师演示、学生提交、只读观察和阶段性快照。
- **实时多人协作**：在操作日志成熟后引入 CRDT、在线光标和角色权限。

### P2：渲染与可视化

- SVG 保留精确交互与可访问性，轨迹、网格和大批量对象转由 Canvas/WebGL 渲染。
- 参数扫描热力图、轨迹密度图、误差可视化及多个构型对比视图。

## 远期：9 个月以上

### P0：受控 AI

- **自然语言作图**：把“作三角形 ABC 的内切圆”等请求转换为可预览、可验证、可撤销的结构化命令。
- **苏格拉底式辅导**：AI 读取对象图、练习目标和步骤历史，优先给下一步提示而非直接答案。
- **自动出题与讲解**：生成初始图、允许工具、检查规则、变式和解析，并由几何验证器把关。

### P1：严格证明

- 引入符号坐标、吴方法或 Gröbner 基等代数工具；机器证明结果再转换为人类可读的定理链。
- 探索导出 Lean/Isabelle 可检查证明，但不把数值采样结果标记为严格证明。

### P2：新空间与生态

- **3D 动态几何**：独立三维对象内核、空间约束、截面、旋转体和 WebGL 相机交互。
- **数据驱动几何实验室**：大规模参数扫描、蒙特卡洛反例和 GPU 加速可视化。
- **社区生态**：共享自定义工具、活动模板、证明规则和主题，并建立签名、审核和兼容策略。
- **AR 几何实验**：从相机画面提取线、圆和角；作为实验功能，不承诺测量级精度。

## 推荐实施顺序

1. 稳定交点身份、统一几何谓词和性能基线。
2. 建立类型化命令日志和复合事务。
3. 上线步骤时间线、练习模式、只读分享与 PWA。
4. 实现增量求值、自定义工具和移动端精细选点。
5. 增加批注、课堂会话，再推进实时多人协作。
6. 让 AI 与证明系统调用同一套命令、对象图和验证接口。
7. 在二维平台稳定后，再独立推进 3D、GPU 和 AR。

## 风险边界

- **严格证明与数值验证必须分开标识**，不能把容差测试包装成数学证明。
- **AI 不得直接修改 DOM 或绕过几何验证器**；所有操作必须可预览、可撤销并记录来源。
- **约束求解器必须显示无解、多解、欠约束和过约束状态**，不能在拖动时静默跳到另一组解。
- **多人协作先合并几何命令，后同步界面状态**；否则容易产生无法解释的对象冲突。
- **性能优化必须以基准为依据**，避免为少量对象牺牲可访问性和编辑精度。
- **课堂与 AI 数据默认最小化采集**，学生内容、身份和模型请求需要清晰的权限与保留策略。
- **3D 和 AR 使用独立内核边界**，不为兼容远期功能提前污染当前二维对象模型。
