> **核心包说明（2026-07-29）：** 本压缩包只交接 SketchpadNext 画板本体。experiments/、examples/、自动出题及证明实验和对应测试已主动排除。基础回归测试为 `tests/geometry.test.js`。

# SketchpadNext 开发交接说明

> 给下一位开发者或新对话中的 AI：请先完整阅读本文件，再阅读 `README.md`、`FEATURES.md` 和 `GSP_MANUAL_PARITY.md`，之后运行全部测试。不要仅根据界面猜测数据结构，也不要在未确认产品决策前恢复已经明确删除的功能。

## 1. 项目目标

SketchpadNext 是一个浏览器原生的现代动态几何画板，目标是覆盖《几何画板 5.06》常用的动态构造、选择、标识、度量、变换、轨迹、演示和多页面工作流，同时改善旧软件的默认样式、交互反馈和现代浏览器体验。

这是净室重写项目，不复制旧软件代码或资源，也不兼容专有 `.gsp` 二进制格式。当前实现使用原生 HTML、CSS、SVG 和 ES Module，不依赖第三方运行时软件包。

## 2. 用户已经明确的产品决策

以下决定不要自行推翻：

- 不做独立多边形工具和多边形内部。
- 不做线标签和圆标签；保留点标签与独立文本。
- 不做 SVG、PNG 等图片导出；用户使用系统截图工具。
- 交点不是独立左侧工具，属于选择工具：在交叉附近单击时建立精确动态交点。
- 坐标网格默认不出现，只有显式创建坐标系后才显示。
- 角相关的三点顺序固定为“第一边上的点—顶点—第二边上的点”，顶点位于中间。
- 一次用户操作只产生一个撤销记录。
- 点大小、线宽、颜色、虚实线等默认样式需要持久化。
- 用户希望交互尽量接近几何画板手册，但明显过时或低价值的功能可以在沟通后删减。

## 3. 启动方式

用户运行环境：Windows 10/11 与最新版 Chrome 或 Edge。只有修改源码和重新生成单文件版时才需要 Node.js 18 或更高版本。

最简单的启动方法：

1. 双击 `SketchpadNext.html`。
2. 浏览器直接打开可使用的画板；不需要 CMD、Node.js 或本地服务。

开发时可以在项目目录运行：

```powershell
npm.cmd start
```

然后手动打开：

```text
http://127.0.0.1:4173/
```

不要直接双击源码入口 `index.html`。浏览器会阻止它的本地 ES Module；用户应打开已生成的 `SketchpadNext.html`。

修改 `index.html` / `styles.css` / `src/` 后，运行 `npm.cmd run build:standalone` 同步生成 `SketchpadNext.html`。批处理脚本仅作开发诊断备用；`server.mjs` 在监听成功后通过 `explorer.exe` 打开默认浏览器。

## 4. 测试

运行：

```powershell
npm.cmd test
```

当前共有 **67 项自动测试**，最后一次交接前运行全部通过。测试文件为：

```text
tests/geometry.test.js
```

测试覆盖：

- 投影、直线/圆交点和定义域。
- 动态父子依赖与级联删除。
- 中点、平行线、垂线、中垂线、角平分线。
- 过三点圆、动态圆心和旧工程迁移。
- 圆弧、内部、退化构造拒绝。
- 角标记、直角方框、反向角和多重弧。
- 文本、度量、参数、计算、表格。
- 变换、轨迹、绘图、动作按钮。
- 复制构造依赖、图片对象和序列化。
- 派生点整体平移、混合绝对坐标对象拖动、局部样式修改和多行命中区域。
- 表达式显式绑定与旧数据迁移、跨类型绘制顺序、双指视图变换数学。

修改核心几何或文档模型后，至少执行：

```powershell
node --check .\src\app.js
node --check .\src\core\document.js
npm.cmd test
```

## 5. 文件结构

```text
SketchpadNext/
├─ SketchpadNext.html
├─ index.html
├─ styles.css
├─ build-standalone.mjs
├─ server.mjs
├─ package.json
├─ 启动 SketchpadNext.cmd
├─ 启动 SketchpadNext.bat
├─ README.md
├─ FEATURES.md
├─ GSP_MANUAL_PARITY.md
├─ DEVELOPMENT_HANDOFF.md
├─ src/
│  ├─ app.js
│  └─ core/
│     ├─ document.js
│     ├─ expression.js
│     ├─ geometry.js
│     ├─ history.js
│     └─ view.js
├─ references/
│  └─ 几何画板使用手册.pdf
└─ tests/
   └─ geometry.test.js
```

文件职责：

- `SketchpadNext.html`：生成的单文件用户版，可直接双击，不需要 CMD、Node.js 或本地服务。
- `index.html`：工具栏、菜单、属性检查器、画布与应用内对话框。
- `styles.css`：整体布局、工具状态、SVG对象样式和响应式界面。
- `src/app.js`：界面状态、选择与拖动、构造命令、渲染、保存/打开和快捷键。目前文件较大，后续可逐步拆分，但拆分前必须保持行为测试。
- `src/core/document.js`：对象模型、动态依赖、几何求值、命中测试、度量文字、序列化、复制和旧数据升级。
- `src/core/geometry.js`：无状态几何算法。
- `src/core/expression.js`：不使用 `eval` 的安全表达式解析和计算。
- `src/core/history.js`：完整文档快照形式的撤销/重做。
- `src/core/view.js`：鼠标缩放、画布平移和双指手势共用的纯视图变换。
- `build-standalone.mjs`：将 HTML、CSS 和 ES Module 打包到 `SketchpadNext.html`，不使用第三方依赖。
- `server.mjs`：无依赖本地静态服务器，默认端口 `4173`。

## 6. 当前已完成的主要能力

### 几何和选择

- 自由点、路径约束点、动态交点。
- 线段、直线、射线、圆、动态半径圆、过三点圆。
- 中点、中垂线、平行线、垂线、角平分线。
- 圆弧、过三点弧、圆内部、扇形内部、弓形内部。
- 累加选择、再次点击取消、Shift 增减、空白框选。
- 自由点和路径点直接拖动；派生点、线、圆和依赖图形通过可移动父对象整体平移。
- 文本、图片、数据对象、手绘和自由原点坐标系支持混合多选拖动。
- 中键、右键或空格加左键平移，连续滚轮缩放；触屏支持双指平移和捏合缩放。
- 隐藏、显示全部、锁定、跨图片/图形/点/文字的全局图层顺序、级联删除。

### 标签、文本和外观

- 点名修改、Unicode、`A[1]` 下标写法、批量命名。
- 点标签可绕点有限距离拖动。
- 点大小、点颜色、线宽、线颜色、实线/虚线和标签默认值。
- 独立文本创建、拖动、双击编辑。
- 编辑已有文本并清空时，直接删除文本对象；该操作可以撤销，不保留不可见空对象。
- 本地图片插入、选择、拖动、复制和工程内保存。

### 标识、度量和信息

- 标识笔从顶点拖入角内创建角标记。
- 90° 自动显示直角方框。
- 1～4 道角弧、大小、透明度、方向箭头和反向角。
- 线段等长刻痕、平行箭头、位置拖动。
- 手绘标记和擦除。
- 信息工具显示对象类型、父对象、子对象、位置、半径或角度。
- 距离、线段长、弧长、线段比、角度、半径、周长、面积、坐标、斜率和路径参数。
- 角标记、圆弧以及任意两条线形对象可以直接测角。
- 度量文字包含具体对象名，例如 `距离 AB`、`线段 AC 长度`、`∠ABC`、`圆 D 半径`；点重命名后文字动态更新。

### 其他能力

- 平移、旋转、缩放、反射和重复最近变换。
- 动态轨迹、点追踪和痕迹清除。
- 参数、安全表达式计算、坐标系、函数图像和参数曲线；表达式对象保存显式参数 ID 依赖。
- 数据表格和当前值采集。
- 隐藏、显示、动画、移动、链接和声音动作按钮。
- 自定义工具保存与依赖重建。
- 多页面新建、切换、改名和删除。
- `Ctrl+C`、`Ctrl+V`、`Ctrl+D` 复制工作流。

详细清单以 `FEATURES.md` 为准；手册对照以 `GSP_MANUAL_PARITY.md` 为准。

## 7. 工程文件和保存语义

可编辑工程扩展名为 `.spn`，内部是开放 JSON，包含全部页面、对象、样式和动态依赖。旧版单页面或多页面 `.json` 仍可打开。

保存规则：

- 新建工程没有文件句柄，第一次“保存”会选择路径，相当于首次另存为。
- 保存或打开 `.spn` 后，“保存”及 `Ctrl+S` 写回当前文件。
- `Ctrl+Shift+S` 和“另存为”创建新文件。
- 当前文件句柄通过 IndexedDB 保存，刷新后会恢复文件关联。
- 刷新后的第一次写回可能出现浏览器“允许修改文件”权限提示，但不应再次选择目录和文件名。
- Chrome/Edge 支持直接写回；不支持 File System Access API 的浏览器只能退化为下载。
- `Ctrl+S` 已被应用拦截，不应出现浏览器的“保存网页”窗口。

相关本地键：

```text
sketchpad-next.settings.v1
sketchpad-next.autosave.v1
sketchpad-next.custom-tools.v1
```

文件句柄数据库：

```text
sketchpad-next.files.v1
```

自动保存恢复的是浏览器里的工作内容；`.spn` 是用户可传递的真实工程文件。不要把两者混为一谈。

## 8. 近期重要修复与数据兼容

- 过三点圆会创建一个 `definition.kind === "circumcenter"` 的动态圆心点。
- 载入旧工程时，`document.js` 的旧数据升级逻辑会给没有圆心的过三点圆补圆心。
- 过三点圆的菜单预检曾遗留多余圆心，已改为一次正式构造。
- 退化的零半径圆、重合点直线、共线三点弧会被拒绝。
- 圆上弧要求起止点确实位于该圆上，避免端点被静默投影后与原点脱节。
- 无效构造不再写入多余撤销快照。
- 度量文字由泛称改为动态对象名称。
- 清空文本改为删除对象，不再保留白色背景下不可见的空文本。
- 拖动判定已经统一：自由点和路径点直接移动；中点、交点、圆心及变换点等派生点会平移其可移动自由依赖；文本、图片、数据对象、手绘和自由原点坐标系可混合拖动。
- 网格吸附只用于几何落点和点拖动，不再让文本、图片和标签表现为“拖不动”。
- “度量 → 角度”的未定义变量崩溃已修复，菜单命令增加统一错误提示。
- 动态圆弧会持续检查端点是否仍在圆上；依赖变化后脱离圆周时返回未定义，不再静默投影。
- 新建工程会检查所有页面是否有内容；已选对象的样式修改不再污染新对象的默认样式。
- 补齐了手绘实时预览、多行文字与动作按钮命中范围、自动保存容量异常处理和对话框焦点恢复。
- 计算只保留表达式实际使用的依赖；动态坐标点、函数图像和参数曲线保存变量到对象 ID 的显式绑定，旧文件会按原名称解析规则补齐。
- `paintOrder` 与对象创建顺序分离，旧文件按原固定视觉层迁移；前后层级、命中测试、复制和序列化现在使用同一套跨类型顺序。
- 触屏首指在产生文档副作用前进行仲裁，双指可同时平移和捏合；滚轮缩放改为连续比例，指针取消会回滚未提交拖动。

保持这些兼容逻辑；不要仅为了精简代码而删除旧数据升级。

## 9. 已知边界和后续建议

这些不一定是错误，但接手者应知晓：

- `src/app.js` 已超过十万字节，后续适合按渲染、交互、文件、菜单命令拆分；拆分要小步进行并保持 67 项测试。
- 当前自动测试主要覆盖核心模型和纯视图变换；本轮之前已人工回归派生点拖动、角度度量、分步 Esc、样式默认值隔离和对话框焦点，文件选择与真实触屏设备仍应人工检查。
- 文件句柄持久化依赖 Chromium 的 File System Access API 和 IndexedDB。
- 两条没有明确角区域的线直接测角时，返回较小夹角；需要有方向或反角语义时，应先创建角标记再测量。
- 独立多边形、线/圆标签、图片导出和 `.gsp` 兼容是明确不做，而非遗漏。
- 当前只有无第三方依赖的单文件生成步骤，没有依赖锁文件；不要无理由引入大型框架。
- 如果增加新对象类型，需要同步检查：对象集合、几何求值、依赖、命中测试、选择框、渲染、复制、序列化、删除级联和测试。

## 10. 下一位开发者开始工作的建议顺序

1. 解压后阅读本文件和三份项目说明。
2. 运行 `npm.cmd test`，确认 67 项通过。
3. 双击 `SketchpadNext.html`，检查页面可操作。
4. 人工检查以下链路：
   - 新建三点和过三点圆，确认出现动态圆心。
   - 创建角标记并执行“度量 → 角度”。
   - 创建度量后修改点名，确认度量文字同步。
   - 创建中点并拖动它，确认两个自由端点和中点一起平移。
   - 修改已选点的半径，取消选择后新建点，确认新点仍使用修改前的默认半径。
   - 建立依赖参数的函数图像，再创建同名参数并复制图像，确认原图与复制品不改绑。
   - 让图片、点和文字重叠，执行移到最前/最后，确认显示与点击对象同步改变。
   - 在真实触屏设备上双指平移、捏合并以不同顺序抬指，确认不会误建点或结束待完成构造。
   - 创建文本后清空，确认对象删除且一次撤销能恢复。
   - 保存 `.spn`，刷新，修改，再按 `Ctrl+S`，确认不出现另存为。
5. 每次修改后先补测试，再运行完整测试。

## 11. 向新对话提供的推荐开场指令

可以把下面这段直接发给新的开发对话：

```text
请先完整阅读项目根目录的 DEVELOPMENT_HANDOFF.md，再阅读 README.md、FEATURES.md 和 GSP_MANUAL_PARITY.md。先运行全部测试并启动本地页面确认当前基线，不要重新实现用户已经明确删除的多边形、线/圆标签和图片导出功能。之后基于交接文档继续开发，并保持现有 .spn 数据兼容和一次操作一个撤销记录。
```

## 12. 本交接包应该包含什么

必须包含整个 `SketchpadNext` 文件夹中的以下内容：

- `index.html`
- `SketchpadNext.html`
- `styles.css`
- `build-standalone.mjs`
- `server.mjs`
- `package.json`
- 两个仅供开发诊断的 Windows 启动脚本
- `src/` 全部源码
- `tests/` 全部测试
- `README.md`
- `FEATURES.md`
- `GSP_MANUAL_PARITY.md`
- `DEVELOPMENT_HANDOFF.md`

不需要包含：

- `node_modules`（项目没有第三方依赖）
- 上级目录里的旧版几何画板安装文件
- `.gsp` 题目、论文、Python转换器或其他个人文件
- 浏览器缓存和自动保存数据

如果需要把用户画好的具体画板一并交接，应另外在应用中保存为 `.spn`，再手动放进压缩包；源码包本身不会读取浏览器自动保存内容。

私人开发交接包还可以额外包含：

```text
references/几何画板使用手册.pdf
```

该手册只作为功能和交互对照资料，不参与程序运行。若接收者已有合法副本，可不重复传递；若压缩包准备公开发布，应移除该 PDF，只保留 `GSP_MANUAL_PARITY.md` 中的净室功能对照。
